# API Captain Release Artifacts

This repository intentionally stores only release distribution metadata.

Version: `v0.0.2`

- mac: `release/mac`
- windows: `release/windows`

To fetch installers into this folder, run:

```bash
./release/download-installers.sh
```
